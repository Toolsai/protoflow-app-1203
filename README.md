# Project Context: new project

> Generated by Pro Prototype on 2025-12-27T09:03:46.402Z

## Overview
This is a functional high-fidelity prototype consisting of 7 interconnected screens. The code is built with HTML5 and Tailwind CSS.

## Navigation Logic (IMPORTANT)
- Navigation is NOT handled via standard `<a href>` tags.
- It uses a declarative attribute: `data-goto="TARGET_PAGE_ID"`.
- A JavaScript shim handles click events on these elements and redirects to `{PAGE_ID}.html`.
- When extending this project, maintain this pattern or refactor all `data-goto` attributes to standard links.

## Screen Topology

### [p1] 啟動頁 (Splash Screen)
- **Filename**: `p1.html`
- **Description**: 藝術與數字的交匯：全螢幕 Three.js 畫布展示旋轉的紅球、黃方塊與藍三角，中央顯示 Bauhaus Ledger 字樣。點擊任意處進入下一步。
- **Outgoing Connections**:
  - links to -> `p2` (歡迎與登入 (Login/Onboarding))

### [p2] 歡迎與登入 (Login/Onboarding)
- **Filename**: `p2.html`
- **Description**: 歡迎進入：採用不對稱網格佈局，包含兩條粗黑底線的極簡輸入框與一個大型黃色圓形登入按鈕。
- **Outgoing Connections**:
  - links to -> `p1` (啟動頁 (Splash Screen))
  - links to -> `p3` (儀表板 (Dashboard))

### [p3] 儀表板 (Dashboard)
- **Filename**: `p3.html`
- **Description**: 財務概覽：視覺核心為 3D 幾何環形圖表，上方顯示大字重餘額「$12,580」，下方設有固定底部導覽列。
- **Outgoing Connections**:
  - links to -> `p7` (個人設定 (Settings & Profile))
  - links to -> `p4` (新增交易 (Add Transaction))
  - links to -> `p5` (數據分析 (Analysis))
  - links to -> `p6` (交易明細 (History Ledger))
  - links to -> `p3` (儀表板 (Dashboard))

### [p4] 新增交易 (Add Transaction)
- **Filename**: `p4.html`
- **Description**: 紀錄收支：具備巨大的網格數字鍵盤，分類選擇使用正方形（餐飲）、圓形（交通）、三角形（娛樂）等幾何圖示。
- **Outgoing Connections**:
  - links to -> `p3` (儀表板 (Dashboard))

### [p5] 數據分析 (Analysis)
- **Filename**: `p5.html`
- **Description**: 幾何趨勢：使用 3D 彩色長方體柱狀圖展示支出，支持左右滑動切換月份，並列出詳細的分類佔比。
- **Outgoing Connections**:
  - links to -> `p3` (儀表板 (Dashboard))
  - links to -> `p5` (數據分析 (Analysis))
  - links to -> `p4` (新增交易 (Add Transaction))
  - links to -> `p6` (交易明細 (History Ledger))
  - links to -> `p7` (個人設定 (Settings & Profile))

### [p6] 交易明細 (History Ledger)
- **Filename**: `p6.html`
- **Description**: 歷史紀錄：以粗黑垂直線作為時間軸，每筆交易顯示為白色方塊，具備鮮明的陰影與邊框細節。
- **Outgoing Connections**:
  - links to -> `p3` (儀表板 (Dashboard))
  - links to -> `p6` (交易明細 (History Ledger))
  - links to -> `p5` (數據分析 (Analysis))
  - links to -> `p4` (新增交易 (Add Transaction))
  - links to -> `p7` (個人設定 (Settings & Profile))

### [p7] 個人設定 (Settings & Profile)
- **Filename**: `p7.html`
- **Description**: 個人偏好：包豪斯海報風格的排版，包含幾何切割的頭像區域、功能清單以及顯眼的紅色矩形登出按鈕。
- **Outgoing Connections**:
  - links to -> `p3` (儀表板 (Dashboard))
  - links to -> `p7` (個人設定 (Settings & Profile))
  - links to -> `p2` (歡迎與登入 (Login/Onboarding))
  - links to -> `p6` (交易明細 (History Ledger))
  - links to -> `p5` (數據分析 (Analysis))

